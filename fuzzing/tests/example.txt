connection_queue_length=10

server {
	server_name=test
	listen=8080

	client_max_body_size=10

	http_redirection=/



	location /test {
		root=html
		index=index.html
		cgi_path=/cgi-bin
		cgi_ext=.py .sh
	}
}

server {
	server_name=server2
	listen=8000
	listen=25565
	listen=9000

	client_max_body_size=3

	http_redirection=/

	location / {
		root=html
		index=index.html
	}
}
